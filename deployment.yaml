apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-arc
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-arc
  template:
    metadata:
      labels:
        app: hello-arc
    spec:
      containers:
      - name: hello-arc
        image: nginx:1.25
        ports:
        - containerPort: 80
